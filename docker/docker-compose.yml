name: enterprise-payment-reconciliation

services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_ROOT_PASSWORD: root123
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -uroot -proot123 --silent"]
      interval: 10s
      timeout: 5s
      retries: 15

  mongo:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  zookeeper:
    image: confluentinc/cp-zookeeper:7.7.1
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000

  kafka:
    image: confluentinc/cp-kafka:7.7.1
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1

  discovery-server:
    build:
      context: ../backend
      dockerfile: Dockerfile
      args:
        MODULE: discovery-server
    ports:
      - "8761:8761"

  auth-service:
    build:
      context: ../backend
      dockerfile: Dockerfile
      args:
        MODULE: auth-service
    depends_on:
      mysql:
        condition: service_healthy
      discovery-server:
        condition: service_started
    environment:
      AUTH_DB_URL: jdbc:mysql://mysql:3306/payment_auth_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      MYSQL_USER: root
      MYSQL_PASSWORD: root123
      EUREKA_URL: http://discovery-server:8761/eureka
      JWT_SECRET: docker-local-secret-change-me-to-a-long-256-bit-value

  transaction-service:
    build:
      context: ../backend
      dockerfile: Dockerfile
      args:
        MODULE: transaction-service
    depends_on:
      mysql:
        condition: service_healthy
      kafka:
        condition: service_started
      discovery-server:
        condition: service_started
    environment:
      TRANSACTION_DB_URL: jdbc:mysql://mysql:3306/payment_transaction_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      MYSQL_USER: root
      MYSQL_PASSWORD: root123
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      EUREKA_URL: http://discovery-server:8761/eureka

  fraud-detection-service:
    build:
      context: ../backend
      dockerfile: Dockerfile
      args:
        MODULE: fraud-detection-service
    depends_on:
      mongo:
        condition: service_started
      kafka:
        condition: service_started
      discovery-server:
        condition: service_started
    environment:
      MONGO_URI: mongodb://mongo:27017/payment_fraud_db
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      EUREKA_URL: http://discovery-server:8761/eureka

  reconciliation-service:
    build:
      context: ../backend
      dockerfile: Dockerfile
      args:
        MODULE: reconciliation-service
    depends_on:
      mysql:
        condition: service_healthy
      kafka:
        condition: service_started
      discovery-server:
        condition: service_started
    environment:
      RECONCILIATION_DB_URL: jdbc:mysql://mysql:3306/payment_reconciliation_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      MYSQL_USER: root
      MYSQL_PASSWORD: root123
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      EUREKA_URL: http://discovery-server:8761/eureka

  notification-service:
    build:
      context: ../backend
      dockerfile: Dockerfile
      args:
        MODULE: notification-service
    depends_on:
      mysql:
        condition: service_healthy
      kafka:
        condition: service_started
      discovery-server:
        condition: service_started
    environment:
      NOTIFICATION_DB_URL: jdbc:mysql://mysql:3306/payment_notification_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      MYSQL_USER: root
      MYSQL_PASSWORD: root123
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      EUREKA_URL: http://discovery-server:8761/eureka

  reporting-service:
    build:
      context: ../backend
      dockerfile: Dockerfile
      args:
        MODULE: reporting-service
    depends_on:
      discovery-server:
        condition: service_started
    environment:
      EUREKA_URL: http://discovery-server:8761/eureka

  api-gateway:
    build:
      context: ../backend
      dockerfile: Dockerfile
      args:
        MODULE: api-gateway
    depends_on:
      discovery-server:
        condition: service_started
    environment:
      EUREKA_URL: http://discovery-server:8761/eureka
      JWT_SECRET: docker-local-secret-change-me-to-a-long-256-bit-value
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ../frontend
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: http://localhost:8080
    depends_on:
      api-gateway:
        condition: service_started
    ports:
      - "5173:80"

volumes:
  mysql-data:
  mongo-data:
