CREATE DATABASE IF NOT EXISTS payment_reconciliation_db;
USE payment_reconciliation_db;

-- The application creates tables automatically with Hibernate ddl-auto=update.
-- Default users are inserted by DataInitializer when the application starts.
