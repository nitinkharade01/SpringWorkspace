server:
  port: 8086

spring:
  application:
    name: reporting-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

app:
  redis:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus

